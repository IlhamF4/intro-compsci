def make_num(a,b,c):
	""" a, b, c are positive ints
    Returns the int number abc, where abc is represented as the concatenation of a, b, and c """
	return str(a) + str(b) + str(c)

print(make_num(12,34,56))